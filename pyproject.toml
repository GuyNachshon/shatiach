[project]
name = "shatiach"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "torch",
    "torchaudio",
    "agno",
    "chromadb",
    "ollama",
    "transformers",
]
